const name = "Mustafizur Rahman is a good boy. He is a Female Person. He Reads in class seven";
const piece = name.split(' ');
const dots = name.split('.');
const single = name.split('');
const cut = name.slice(5, 9);
const subSet = name.substring(9, 15);
// console.log(dots);
const newPara = dots.join(':');
console.log(newPara);